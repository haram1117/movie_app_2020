import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

//위 코드에서 <App />을 component라고 함,, component: html을 반환하는 함수 => jsx!!